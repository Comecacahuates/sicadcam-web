---
import type { HTMLAttributes } from "astro/types";

export interface Props extends HTMLAttributes<"div"> {}

const { class: _class, ...rest } = Astro.props;
---

<div
  class:list={["bg-white rounded-lg shadow-md border border-slate-200", _class]}
  {...rest}
>
  <slot />
</div>
